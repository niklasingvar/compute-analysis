---
description: Read root CLAUDE.md for project conventions (SSOT); do not duplicate here.
alwaysApply: true
---

# Cursor rules → `CLAUDE.md` as the single source

- **Single source of truth** for stack, Next.js gotchas, hydration requirements, file layout, and meta-rules is [`CLAUDE.md`](../../CLAUDE.md) at the repo root (same substance as the workspace *always_applied_workspace_rules* entry when it is loaded).
- Before larger changes under `app/` or analysis markdown: **read the relevant sections in `CLAUDE.md`** (e.g. **Hydration** under the web app) instead of relying on short rule files alone.
- **Do not** paste long copies of those conventions into `.cursor/rules/` — update `CLAUDE.md` only so SSOT stays intact.
